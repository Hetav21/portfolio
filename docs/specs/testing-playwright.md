# Playwright Testing Specification

The Playwright testing framework is integrated into the Bun development environment flake. Node.js is kept alongside Bun for Playwright compatibility. Browsers are managed via Nix environment variables. Bun is the sole package manager for executing test scripts.
